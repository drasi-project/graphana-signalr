apiVersion: 1

datasources:
  - name: Drasi SignalR
    type: drasi-signalr-datasource
    access: proxy
    url: http://localhost:8002
    isDefault: false
    jsonData:
      signalrUrl: 'http://localhost:8002/hub'
      queryIds: ['freezerx']
    editable: true